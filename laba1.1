/* первая лаба, перезалил на новую ветку, 
старый файл удалю с ветки main */


#include <iostream>
#include<cmath>
#include<iomanip>
using namespace std;
int main()
{
    setlocale(LC_ALL, "Rus");
    double ans,step,x,m;
	int k;
	cout<<"Введите  x(-1;1]:";
	cin>>x;
	if(x<=-1 || x>1)
	{
		do
	{
		cout<<"Вы ввели число, которое не входит в область определения функции, повторите попытку"<<endl;
		cout<<"Введите x(-1;1]: ";
		cin>>x;
	}	while(x<=-1 || x>1);
	}
	cout<<"Введите k:";
	cin>>k;
	step= pow(10,-k);
	double slog;
	slog=x;
	int n = 2;
	do
	{
		ans+=slog;
	slog *= (-1)*x/n;
	n+=1;
}while(abs(slog)>step);

cout<<"Ответ:"<<fixed<<setprecision(k)<<ans<<endl;
cout<<"ln(1+x):"<<fixed<<setprecision(k)<<log(1+x)<<endl;
cout<<"Разность значений:"<<abs(ans-log(1+x));
return 0;
}
